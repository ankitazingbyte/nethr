<%= form_with(model: award, local: true) do |form| %>
  <% if award.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(award.errors.count, "error") %> prohibited this award from being saved:</h2>

      <ul>
      <% award.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :award_name %>
  </div>

  <div class="field">
    <%= form.label :gift %>
    <%= form.text_field :gift, id: :award_gift %>
  </div>

  <div class="field">
    <%= form.label :cash_price %>
    <%= form.number_field :cash_price, id: :award_cash_price %>
  </div>

  <div class="field">
    <%= form.label :month %>
    <%= form.text_field :month, id: :award_month %>
  </div>

  <div class="field">
    <%= form.label :year %>
    <%= form.text_field :year, id: :award_year %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
