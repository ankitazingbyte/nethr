<%= form_for @admin_document, html: { multipart: true } do |form| %>
  <% if admin_document.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_document.errors.count, "error") %> prohibited this admin_document from being saved:</h2>

      <ul>
      <% admin_document.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :admin_document_name %>
  </div>

  <div class="field">
    <%= form.label :file %>
    <%= form.file_field :attachment %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
