<%= form_with(model: admin_notice, local: true) do |form| %>
  <% if admin_notice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_notice.errors.count, "error") %> prohibited this admin_notice from being saved:</h2>

      <ul>
      <% admin_notice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :admin_notice_title %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status, id: :admin_notice_status %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :admin_notice_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
