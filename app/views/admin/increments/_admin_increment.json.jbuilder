json.extract! admin_increment, :id, :admin_employee_id, :admin_designation_id, :current_salary, :increment_salary, :created_at, :updated_at
json.url admin_increment_url(admin_increment, format: :json)
