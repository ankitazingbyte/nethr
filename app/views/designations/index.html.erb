<main id="wrapper" class="wrapper">
    <section class="wrapper-bottom-sec">
        <div class="p-30">
            <h2 class="page-title">Designation</h2>
        </div>
        <div class="p-30 p-t-none p-b-none">
            <div class="row">
                <div class="col-lg-4">
                    <div class="panel">
                        <div class="panel-body">
                            <div class="panel-heading">
                                <h3 class="panel-title">Add Designation</h3>
                            </div>
                            <div class="form-group">
                                <label>Designation Name</label>
                                <span class="help">e.g."Account Designation"</span>
                                <%= render "designations/form" %>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="col-lg-8">
                    <div class="panel">
                        <div class="panel-heading">
                            <h3 class="panel-title">All Designations</h3>
                        </div>
                        <div class="panel-body p-none">
                            <table class="table data-table table-hover table-ultra-responsive">
                                <thead>
                                    <tr>
                                        <th style="width: 10%;">SL#</th>
                                        <th style="width: 30%;">Designation Name</th>
                                        <th style="width: 30%;">Department Name</th>
                                        <th style="width: 35%;">Actions</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% @designations.each do |designation| %>
                                        <tr>
                                            <td><%= designation.id %></td>
                                            <td><%= designation.name %></td>
                                            <td><%= designation.department.name %></td>
                                            <td>
                                                <%= link_to 'Edit', edit_designation_path(designation), class: "btn btn-success btn-xs" %>
                                                <%= link_to 'Destroy', designation, method: :delete, data: { confirm: 'Are you sure?' },class: "btn btn-danger btn-xs" %>
                                            </td>
                                        </tr>
                                    <% end %>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
</main>